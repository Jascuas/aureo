# Legacy Rules Compatibility Pointer

This file is retained only because older local OpenCode profiles may reference
it. It is not an independent rules file.

Read `AGENTS.md` for the complete normative coding, architecture, security,
workflow, and verification contract. Read `docs/PRODUCT.md` and
`docs/DESIGN.md` when their subject applies. Approved deviations exist only in
`docs/EXCEPTIONS.md`.

The remaining documents under `.opencode/docs/` are historical research or
compatibility references. Their commands, examples, package versions, and
recommendations may be stale and never override the canonical documents.
